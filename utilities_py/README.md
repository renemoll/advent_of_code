# Advent of Code utilities

The utilities library for Python.

## Development

Run the unit-tests:

```bash
uv run pytest
```

With a coverage report:

```bash
uv run pytest --cov
uv run coverage html
```
